# Frontend AxisClip

Frontend React/TypeScript servido por Vite.

A documentacao principal do projeto esta no `README.md` da raiz e em `docs/`.

Comandos locais:

```bash
npm install
npm run dev
npm run build
npm run lint
```
